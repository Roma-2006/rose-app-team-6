# TODO

- [ ] Update MostPopularSection behavior for the tabs/“home.all” state
  - Ensure default load shows "All" but products are not fetched as `undefined`/wrong param.
  - Map “All” button to a concrete value that backend understands (e.g., occasionId for home.all) OR handle it explicitly in `queryFn`.

- [ ] Run/build/test (or at least `next lint` / `tsc`) to ensure no type errors.

